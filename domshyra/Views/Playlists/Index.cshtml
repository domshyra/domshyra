@using domshyra.Models;
@using System.Collections.Generic
@model List<PlaylistsModel>

@{
    Layout = "_Layout";
    ViewBag.Title = "Radio stations";
}

<style>
    .description-row {
        height: 200px;
        max-height: 200px;
    }

    .playlist-img {
        height: 100%;
        width: 100%;
        max-height: 200px;
    }    

    .card-body-playlist {
        min-height: 250px;
    }
    .card-icon {
        font-size: 1.2rem;
        line-height: 1.2;
    }
</style>

<div class="container">
    <div class="d-none d-xl-block">
        <section class="jumbotron text-center bg-light">
            <div class="container">
                <h1 class="jumbotron-heading">Radio stations</h1>
                <p class="lead text-muted mb-0">
                    Music is the oldest form of story telling and I've always loved a good story
                </p>
                <p class="text-muted font-weight-light">
                    Spotify playlists curated like radio stations
                </p>
                <small class="text-center font-weight-light"><b>Note:</b> Apple music is deprecated and the most up to date playlists are on <a href="https://open.spotify.com/user/domshyra">Spotify</a></small>
            </div>
        </section>
    </div>
    <div class="d-xl-none">

        <h2 class="pb-2">Radio stations</h2>
    </div>

    <div class="row">

        <div id="hello-react">

        </div>

        @foreach (PlaylistsModel playlist in Model)
        {
            <div class="col-12 col-xl-6 mb-2" title="@playlist.Title" aria-label="@playlist.Title" id="@playlist.AnchorId">
                <partial name="_PlaylistCardPartial" model="playlist" />
            </div>
        }
    </div>
</div>


<script src="~/js/main.bundle.js"></script>